import { SerenityBDDReport } from '../../SerenityBDDJsonSchema';
import { EventQueue } from '../EventQueue';
import { EventQueueProcessor } from '../EventQueueProcessor';
/**
 * @package
 */
export declare class SingleSceneEventQueueProcessor extends EventQueueProcessor {
    supports(queue: EventQueue): boolean;
    process(queue: EventQueue): SerenityBDDReport;
    private onSceneStarts;
    private onActivityStarts;
    private onActivityFinished;
    private onSceneFinished;
}

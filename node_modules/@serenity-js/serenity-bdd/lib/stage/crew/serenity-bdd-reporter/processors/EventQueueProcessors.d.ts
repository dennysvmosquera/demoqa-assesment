import { Artifact, CorrelationId, Name } from '@serenity-js/core/lib/model';
import { EventQueues } from './EventQueues';
/**
 * @package
 */
export declare class EventQueueProcessors {
    private readonly singleSceneProcessor;
    private readonly sceneSequenceProcessor;
    process(queues: EventQueues): Array<{
        artifact: Artifact;
        name: Name;
        sceneId: CorrelationId;
    }>;
}
